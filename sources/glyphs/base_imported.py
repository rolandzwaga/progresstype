"""Base glyphs imported from IBM Plex Mono (Latin Core coverage).

Glyphs sourced from IBM Plex Mono (Copyright IBM, SIL OFL 1.1)
https://github.com/IBM/plex
"""

import pickle
import os

from sources.config import UPM, ASCENDER, ASCII_WIDTH, SPACE_WIDTH, DIGIT_WIDTH, CAP_HEIGHT, X_HEIGHT

# Google Fonts Latin Core codepoints (319 codepoints)
LATIN_CORE_CODEPOINTS = [
    0x0020, 0x0021, 0x0022, 0x0023, 0x0024, 0x0025, 0x0026, 0x0027, 0x0028, 0x0029,
    0x002A, 0x002B, 0x002C, 0x002D, 0x002E, 0x002F, 0x0030, 0x0031, 0x0032, 0x0033,
    0x0034, 0x0035, 0x0036, 0x0037, 0x0038, 0x0039, 0x003A, 0x003B, 0x003C, 0x003D,
    0x003E, 0x003F, 0x0040, 0x0041, 0x0042, 0x0043, 0x0044, 0x0045, 0x0046, 0x0047,
    0x0048, 0x0049, 0x004A, 0x004B, 0x004C, 0x004D, 0x004E, 0x004F, 0x0050, 0x0051,
    0x0052, 0x0053, 0x0054, 0x0055, 0x0056, 0x0057, 0x0058, 0x0059, 0x005A, 0x005B,
    0x005C, 0x005D, 0x005E, 0x005F, 0x0060, 0x0061, 0x0062, 0x0063, 0x0064, 0x0065,
    0x0066, 0x0067, 0x0068, 0x0069, 0x006A, 0x006B, 0x006C, 0x006D, 0x006E, 0x006F,
    0x0070, 0x0071, 0x0072, 0x0073, 0x0074, 0x0075, 0x0076, 0x0077, 0x0078, 0x0079,
    0x007A, 0x007B, 0x007C, 0x007D, 0x007E, 0x00A0, 0x00A1, 0x00A2, 0x00A3, 0x00A5,
    0x00A7, 0x00A8, 0x00A9, 0x00AA, 0x00AB, 0x00AE, 0x00AF, 0x00B0, 0x00B4, 0x00B6,
    0x00B7, 0x00B8, 0x00BA, 0x00BB, 0x00BF, 0x00C0, 0x00C1, 0x00C2, 0x00C3, 0x00C4,
    0x00C5, 0x00C6, 0x00C7, 0x00C8, 0x00C9, 0x00CA, 0x00CB, 0x00CC, 0x00CD, 0x00CE,
    0x00CF, 0x00D0, 0x00D1, 0x00D2, 0x00D3, 0x00D4, 0x00D5, 0x00D6, 0x00D7, 0x00D8,
    0x00D9, 0x00DA, 0x00DB, 0x00DC, 0x00DD, 0x00DE, 0x00DF, 0x00E0, 0x00E1, 0x00E2,
    0x00E3, 0x00E4, 0x00E5, 0x00E6, 0x00E7, 0x00E8, 0x00E9, 0x00EA, 0x00EB, 0x00EC,
    0x00ED, 0x00EE, 0x00EF, 0x00F0, 0x00F1, 0x00F2, 0x00F3, 0x00F4, 0x00F5, 0x00F6,
    0x00F7, 0x00F8, 0x00F9, 0x00FA, 0x00FB, 0x00FC, 0x00FD, 0x00FE, 0x00FF, 0x0100,
    0x0101, 0x0102, 0x0103, 0x0104, 0x0105, 0x0106, 0x0107, 0x010A, 0x010B, 0x010C,
    0x010D, 0x010E, 0x010F, 0x0110, 0x0111, 0x0112, 0x0113, 0x0116, 0x0117, 0x0118,
    0x0119, 0x011A, 0x011B, 0x011E, 0x011F, 0x0120, 0x0121, 0x0122, 0x0123, 0x0126,
    0x0127, 0x012A, 0x012B, 0x012E, 0x012F, 0x0130, 0x0131, 0x0136, 0x0137, 0x0139,
    0x013A, 0x013B, 0x013C, 0x013D, 0x013E, 0x0141, 0x0142, 0x0143, 0x0144, 0x0145,
    0x0146, 0x0147, 0x0148, 0x0150, 0x0151, 0x0152, 0x0153, 0x0154, 0x0155, 0x0158,
    0x0159, 0x015A, 0x015B, 0x015E, 0x015F, 0x0160, 0x0161, 0x0164, 0x0165, 0x016A,
    0x016B, 0x016E, 0x016F, 0x0170, 0x0171, 0x0172, 0x0173, 0x0174, 0x0175, 0x0176,
    0x0177, 0x0178, 0x0179, 0x017A, 0x017B, 0x017C, 0x017D, 0x017E, 0x0218, 0x0219,
    0x021A, 0x021B, 0x0237, 0x02C6, 0x02C7, 0x02D8, 0x02D9, 0x02DA, 0x02DB, 0x02DC,
    0x02DD, 0x0300, 0x0301, 0x0302, 0x0303, 0x0304, 0x0306, 0x0307, 0x0308, 0x030A,
    0x030B, 0x030C, 0x0326, 0x0327, 0x0328, 0x1E80, 0x1E81, 0x1E82, 0x1E83, 0x1E84,
    0x1E85, 0x1E9E, 0x1EF2, 0x1EF3, 0x2013, 0x2014, 0x2018, 0x2019, 0x201A, 0x201C,
    0x201D, 0x201E, 0x2022, 0x2026, 0x2039, 0x203A, 0x20AC, 0x2122, 0x2212
]

# Load imported glyph data
_SCRIPT_DIR = os.path.dirname(os.path.abspath(__file__))
_GLYPH_DATA_PATH = os.path.join(os.path.dirname(_SCRIPT_DIR), 'imported_glyphs.pkl')

with open(_GLYPH_DATA_PATH, 'rb') as f:
    _IMPORTED_GLYPHS = pickle.load(f)

# IBM Plex Mono is 600 units wide (monospace), we use 500
# Scale factor to fit our metrics
SCALE_FACTOR = ASCII_WIDTH / 600  # 500 / 600 = 0.833


def _scale_point(point):
    """Scale a point from IBM Plex coordinates to our UPM."""
    return (int(point[0] * SCALE_FACTOR), point[1])


def _draw_imported_glyph(pen, char):
    """Draw an imported glyph from IBM Plex Mono."""
    if char not in _IMPORTED_GLYPHS:
        # Fallback to simple rectangle if glyph not found
        _draw_rect(pen, 50, 0, ASCII_WIDTH - 50, CAP_HEIGHT)
        return

    data = _IMPORTED_GLYPHS[char]
    commands = data['commands']

    for cmd, args in commands:
        if cmd == 'moveTo':
            pen.moveTo(_scale_point(args[0]))
        elif cmd == 'lineTo':
            pen.lineTo(_scale_point(args[0]))
        elif cmd == 'qCurveTo':
            # Quadratic curve - scale all points
            scaled_points = [_scale_point(p) for p in args]
            pen.qCurveTo(*scaled_points)
        elif cmd == 'curveTo':
            # Cubic curve - scale all points
            scaled_points = [_scale_point(p) for p in args]
            pen.curveTo(*scaled_points)
        elif cmd == 'closePath':
            pen.closePath()
        elif cmd == 'endPath':
            pen.endPath()


def _draw_rect(pen, x0, y0, x1, y1):
    """Draw a simple rectangle (fallback)."""
    pen.moveTo((x0, y0))
    pen.lineTo((x1, y0))
    pen.lineTo((x1, y1))
    pen.lineTo((x0, y1))
    pen.closePath()


def _make_notdef(pen):
    """Draw .notdef (empty rectangle)."""
    _draw_rect(pen, 50, 0, 450, CAP_HEIGHT)
    _draw_rect(pen, 100, 50, 400, CAP_HEIGHT - 50)


def _make_block_letter(pen, char):
    """Draw a letter using imported IBM Plex Mono glyph."""
    _draw_imported_glyph(pen, char)




def draw_base_glyphs(glyph_drawing_funcs):
    """Register drawing functions for all base glyphs (Latin Core coverage).

    Args:
        glyph_drawing_funcs: dict to populate with {glyph_name: (width, draw_func)}
    """
    # .notdef
    glyph_drawing_funcs[".notdef"] = (ASCII_WIDTH, _make_notdef)

    # All Latin Core characters - use imported glyphs
    for code in LATIN_CORE_CODEPOINTS:
        char = chr(code)

        # Use standard glyph names for special characters
        if code == 0x0020:  # Space
            name = "space"
            width = SPACE_WIDTH
            glyph_drawing_funcs[name] = (width, None)
            continue
        elif code == 0x00A0:  # Non-breaking space
            name = "uni00A0"
            width = SPACE_WIDTH
            glyph_drawing_funcs[name] = (width, None)
            continue
        else:
            name = f"uni{code:04X}"
            # Digits get digit width, everything else gets ASCII width
            width = DIGIT_WIDTH if char.isdigit() else ASCII_WIDTH

        # Create closure to capture char
        def make_draw(c):
            return lambda pen: _make_block_letter(pen, c)
        glyph_drawing_funcs[name] = (width, make_draw(char))
